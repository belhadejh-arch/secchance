# خطة تنفيذ مشروع منصة الفرصة الثانية — IMPLEMENTATION_PLAN.md

## 1. الأهداف والمعمارية العامة (Architecture)
نظام متكامل يتبع معمارية الطبقات الأربع:
1. **Presentation Layer**: واجهات تفاعلية React 19 + TypeScript + Tailwind CSS متجاوبة تماماً مع دعم كامل للـ RTL ونوافذ العمل المخصصة لكل دور مستخدم.
2. **API Gateway & Middleware Layer**: Express Router v1 (`/api/v1/*`) مع معالجة JWT، التحقق من الصلاحيات (RBAC)، Policies لمنع وصول أي مستخدم أو مختص إلى ملفات غير مصرح بها، Rate Limiting، وValidation صارم.
3. **Business Logic & Services**:
   - `AuthService`: تسجيل، دخول JWT، إدارة الحساب، إعادة تعيين كلمة المرور.
   - `CaseService`: إدارة دورة حياة الحالة (Workflow: NEW -> UNDER_REVIEW -> ASSIGNED -> FIRST_SESSION -> FOLLOW_UP -> REFERRED -> COMPLETED -> ARCHIVED)، واحتساب زمن الاستجابة والأولوية.
   - `AppointmentService`: جدولة مواعيد، منع التعارض، تذكيرات، إدارة الحالات.
   - `PsychologyService`: تقييم أولي، خطة علاجية، جلسات، ونسب التقدم، تقارير نهائية إلزامية قبل الإغلاق.
   - `LegalService`: استشارات قانونية، رفع توصيات ورأي قانوني مع عزل السرية الطبية.
   - `CenterService` & `AssociationService`: استقبال الإحالات، متابعة أسبوعية، تسجيل الخروج.
   - `MessageService`: مراسلات مشفرة مرتبطة بالحالة حصرًا (Case -> Conversation -> Messages).
   - `NotificationService`: إشعارات فورية بالحالة والمواعيد والرسائل.
   - `AuditLogService`: تسجيل العمليات الحساسة لمنع التلاعب وحماية الخصوصية.
   - `AIService`: توجيه أولي، اقتراح تصنيف بدون تقديم تشخيص طبي قطعي باستخدام Gemini SDK.
4. **Data Persistence Layer**: قاعدة بيانات حقيقية SQLite تخزن على القرص، مع بنية الجداول الكاملة، الفهارس، المفاتيح الأجنبية، وSeeders حقيقية تشمل ولايات الجزائر وأنواع الإدمان وحسابات أولية تجريبية لكل دور.

## 2. مراحل التنفيذ التفصيلية
- **المرحلة 1**: بناء مخطط قاعدة البيانات والجداول والعلاقات في ملف `server/db.ts`.
- **المرحلة 2**: بناء نواة المصادقة والأمان والصلاحيات `server/auth.ts` و`server/middleware.ts`.
- **المرحلة 3**: برمجة مسارات REST API v1 الكاملة وفق المواصفات.
- **المرحلة 4**: ربط خدمة الذكاء الاصطناعي Gemini للتوجيه والتصنيف الأولي مع تنبيهات الطوارئ.
- **المرحلة 5**: بناء الواجهات الأمامية التفاعلية:
  - الصفحة الرئيسية العامة (Landing Page) بتصميم هادئ وموثوق وأرقام فعلية مسترجعة من الـ API.
  - شاشات المصادقة والتسجيل واختيار الدور.
  - بوابة الأسرة والمستفيد (تقديم طلب متعدد الخطوات، تتبع الحالة، المواعيد، الرسائل).
  - بوابة الأخصائي النفسي (تقييمات، خطط علاجية، جلسات، تقارير نهائية).
  - بوابة المحامي (استشارات قانونية، رأي قانوني، مستندات).
  - بوابة مراكز العلاج والجمعيات (إحالات، متابعة أسبوعية، خروج).
  - لوحة الإدارة المتكاملة (إحصائيات، إدارة المستخدمين والمختصين، اعتماد الحسابات، تقارير، إعدادات، Audit Logs).
- **المرحلة 6**: توثيق API والاختبارات والتأكد من عدم وجود أي زر معطل أو بيانات وهمية.
